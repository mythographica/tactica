// Stand-in for the generated root instance type. The field pattern's
// intersection alias references the generated alias from the .tactica
// output; the fixture keeps an identically-shaped alias in project
// source so the shape exists during analysis. Synthetic identifiers only.

export type TVaultGeneratedInstance = {
	code: string;
	amount: number;
};
