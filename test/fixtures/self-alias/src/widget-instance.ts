// Stand-in for the generated instance alias. The field pattern imports
// the alias from the generated types file; the fixture keeps an
// identically-shaped alias in project source so analysis sees a plain
// type alias. Synthetic identifiers only.

export type TWidgetInstance = {
	label: string;
	size: number;
};
